<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mateus.a2do_trabalho">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/todologo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NovaContaActivity" />
        <activity android:name=".DrawerActivity" />
        <activity android:name=".CategoriaFormActivity" />
        <activity android:name=".TarefaFormActivity" />
        <activity android:name=".SelecionarCategoriaActivity" />
        <activity android:name=".AlterarUsuarioActivity"></activity>
    </application>

</manifest>